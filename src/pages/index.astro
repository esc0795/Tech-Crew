---
import Layout from "../layouts/Layout.astro";
import Header from "../components/Header.astro";
---

<html>
  <head>
    <meta charset="UTF-8" />
    <title>Tech Crew</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/Tech-Crew/global.css" />
  </head>

  <body>
    <Header />
    <!-- FONDO GLOBAL ANIMADO -->
    <canvas id="global-canvas"></canvas>

    <!-- HERO PRINCIPAL -->
    <div class="hero-section">
      <div class="hero-content">
        <h3>
          CRECEMOS<br />
          INCENTIVANDO<br />
          A CRECER
        </h3>
        <a href="#soluciones" id="cta-button" class="cta-button"
          >Conocer m&aacute;s</a
        >
      </div>
    </div>

    <!-- SECCIÓN DE IMPACTO, ESTADÍSTICAS Y CASOS DE ÉXITO -->
    <section class="stats-section" id="catalogo">
      <div class="stats-header">
        <h2>Impacto real en tu negocio y marca personal</h2>
      </div>

      <div class="stats-grid">
        <!-- Caso 1: Ventas y Tienda con WhatsApp Automatizado -->
        <div class="stat-card">
          <div class="stat-card-top">
            <span class="card-chip">WhatsApp &amp; E-Commerce</span>
            <div class="stat-metric">-75%</div>
            <div class="stat-metric-label">
              Tiempo en atenci&oacute;n y filtrado
            </div>
          </div>
          <div class="stat-card-body">
            <h4>Ventas Automatizadas por WhatsApp</h4>
            <p>
              Elimina la fricci&oacute;n de responder manualmente cada mensaje
              sobre tallas, disponibilidad y precios. Conectamos una tienda web
              donde el cliente elige y env&iacute;a el pedido listo para pagar
              directo a tu WhatsApp.
            </p>
            <div class="stat-features">
              <div class="feature-item">
                <span class="check-icon">&#10003;</span>
                <span
                  >Filtro autom&aacute;tico de tallas, stock y cat&aacute;logo</span
                >
              </div>
              <div class="feature-item">
                <span class="check-icon">&#10003;</span>
                <span>Cierre de venta inmediato sin demoras 24/7</span>
              </div>
            </div>
          </div>
        </div>

        <!-- Caso 2: CVs Web Optimizados para IA / Sistemas ATS -->
        <div class="stat-card">
          <div class="stat-card-top">
            <span class="card-chip">Empleabilidad &amp; Marca</span>
            <div class="stat-metric">+300%</div>
            <div class="stat-metric-label">
              Visibilidad ante reclutadores e IA
            </div>
          </div>
          <div class="stat-card-body">
            <h4>CVs Web Optimizados para IA / ATS</h4>
            <p>
              Los sistemas de reclutamiento actuales filtran candidatos mediante
              Inteligencia Artificial. Dise&ntilde;amos tu CV en l&iacute;nea
              con estructura y metadatos adaptados para superar filtros ATS y
              destacar profesionalmente.
            </p>
            <div class="stat-features">
              <div class="feature-item">
                <span class="check-icon">&#10003;</span>
                <span>Estructura 100% compatible con lectores ATS e IA</span>
              </div>
              <div class="feature-item">
                <span class="check-icon">&#10003;</span>
                <span>Enlace web propio interactivo y de alto impacto</span>
              </div>
            </div>
          </div>
        </div>

        <!-- Caso 3: Menús Web para Restaurantes (To-Go & Salón) -->
        <div class="stat-card">
          <div class="stat-card-top">
            <span class="card-chip">Gastronom&iacute;a Digital</span>
            <div class="stat-metric">+25%</div>
            <div class="stat-metric-label">
              Ticket promedio y rotaci&oacute;n
            </div>
          </div>
          <div class="stat-card-body">
            <h4>Men&uacute;s Web y Pedidos To-Go</h4>
            <p>
              Men&uacute;s interactivos r&aacute;pidos por QR o enlace web.
              Desde cartas digitales informativas hasta sistemas con
              selecci&oacute;n de platillos que cierran la orden directo
              v&iacute;a WhatsApp para sal&oacute;n o servicio to-go.
            </p>
            <div class="stat-features">
              <div class="feature-item">
                <span class="check-icon">&#10003;</span>
                <span
                  >Actualizaci&oacute;n de precios y platos en tiempo real</span
                >
              </div>
              <div class="feature-item">
                <span class="check-icon">&#10003;</span>
                <span
                  >Cero costos de reimpresi&oacute;n y pedidos sin
                  intermediarios</span
                >
              </div>
            </div>
          </div>
        </div>

        <!-- Caso 4: Landing Pages de Alta Conversión -->
        <div class="stat-card">
          <div class="stat-card-top">
            <span class="card-chip">Conversi&oacute;n &amp; Leads</span>
            <div class="stat-metric">3.5x</div>
            <div class="stat-metric-label">
              M&aacute;s conversi&oacute;n de prospectos
            </div>
          </div>
          <div class="stat-card-body">
            <h4>Landing Pages de Alta Conversi&oacute;n</h4>
            <p>
              P&aacute;ginas estrat&eacute;gicas enfocadas en convertir visitas
              en clientes reales. Arquitectura persuasiva y velocidad extrema
              para maximizar el retorno de tu publicidad y presencia web.
            </p>
            <div class="stat-features">
              <div class="feature-item">
                <span class="check-icon">&#10003;</span>
                <span
                  >Enfoque directo en captaci&oacute;n de leads cualificados</span
                >
              </div>
              <div class="feature-item">
                <span class="check-icon">&#10003;</span>
                <span
                  >Carga instant&aacute;nea optimizada para dispositivos
                  m&oacute;viles</span
                >
              </div>
            </div>
          </div>
        </div>

        <!-- Caso 5: Digitalización Integral para Negocios -->
        <div class="stat-card">
          <div class="stat-card-top">
            <span class="card-chip">Transformaci&oacute;n Digital</span>
            <div class="stat-metric">24/7</div>
            <div class="stat-metric-label">
              Presencia activa y tr&aacute;fico controlado
            </div>
          </div>
          <div class="stat-card-body">
            <h4>Digitalizaci&oacute;n Integral de Negocios</h4>
            <p>
              Llevamos tu negocio tradicional, cat&aacute;logo o producto
              f&iacute;sico al ecosistema digital con bajo costo por
              tr&aacute;fico controlado, permiti&eacute;ndote competir y escalar
              con tecnolog&iacute;a moderna.
            </p>
            <div class="stat-features">
              <div class="feature-item">
                <span class="check-icon">&#10003;</span>
                <span>Automatizaci&oacute;n de procesos repetitivos</span>
              </div>
              <div class="feature-item">
                <span class="check-icon">&#10003;</span>
                <span>Estrategias adaptadas al presupuesto de tu negocio</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- SECCIÓN DE SOLUCIONES RENOVADA -->
    <section class="soluciones-section" id="soluciones">
      <div class="soluciones-header">
        <span class="soluciones-badge">Servicios Especializados</span>
        <h2>Soluciones Digitales a tu Medida</h2>
      </div>

      <div class="solution-cards">
        <!-- 1. Landing Pages y SEO -->
        <div class="solution-card">
          <div class="card-header" onclick="toggleCard(this)">
            <div class="header-info">
              <span class="solution-chip"
                >Conversi&oacute;n &amp; Tr&aacute;fico</span
              >
              <span class="solution-title"
                >Landing Pages y Posicionamiento SEO</span
              >
            </div>
            <span class="icon">+</span>
          </div>
          <div class="card-content">
            <div class="card-content-inner">
              <p>
                Dise&ntilde;amos p&aacute;ginas de aterrizaje de alto
                rendimiento combinadas con optimizaci&oacute;n para motores de
                b&uacute;squeda (SEO). Estructuramos cada secci&oacute;n con
                psicolog&iacute;a de conversi&oacute;n y palabras clave
                estrat&eacute;gicas para que tu negocio aparezca en Google y
                convierta visitas en prospectos y clientes reales.
              </p>
              <div class="solution-bullets">
                <div class="solution-bullet">
                  <span class="bullet-check">&#10003;</span>
                  <span
                    >Optimizaci&oacute;n t&eacute;cnica y posicionamiento
                    org&aacute;nico en Google</span
                  >
                </div>
                <div class="solution-bullet">
                  <span class="bullet-check">&#10003;</span>
                  <span
                    >Estructura persuasiva de alta conversi&oacute;n para
                    campa&ntilde;as y anuncios</span
                  >
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- 2. Documentos Digitales (Menús y CVs) -->
        <div class="solution-card">
          <div class="card-header" onclick="toggleCard(this)">
            <div class="header-info">
              <span class="solution-chip">Men&uacute;s QR &amp; CVs con IA</span
              >
              <span class="solution-title"
                >Documentos Digitales Inteligentes (Men&uacute;s y CVs)</span
              >
            </div>
            <span class="icon">+</span>
          </div>
          <div class="card-content">
            <div class="card-content-inner">
              <p>
                Modernizamos la presentaci&oacute;n de tu negocio
                gastron&oacute;mico y tu perfil profesional:
              </p>
              <div class="solution-bullets">
                <div class="solution-bullet">
                  <span class="bullet-check">&#10003;</span>
                  <span
                    ><strong>Men&uacute;s Web para Restaurantes:</strong> Cartas
                    interactivas por c&oacute;digo QR con actualizaci&oacute;n instant&aacute;nea
                    y carrito para recibir pedidos directos a WhatsApp (to-go y sal&oacute;n).</span
                  >
                </div>
                <div class="solution-bullet">
                  <span class="bullet-check">&#10003;</span>
                  <span
                    ><strong>CVs Optimizados para IA / ATS:</strong> Curr&iacute;culums
                    web interactivos con el formato exacto requerido por los algoritmos
                    de reclutamiento para evitar ser descartado y destacar ante empleadores.</span
                  >
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- 3. Sitios Web y Tiendas con WhatsApp -->
        <div class="solution-card">
          <div class="card-header" onclick="toggleCard(this)">
            <div class="header-info">
              <span class="solution-chip"
                >E-Commerce &amp; Cat&aacute;logos</span
              >
              <span class="solution-title"
                >Sitios Web y Tiendas con Cierre en WhatsApp</span
              >
            </div>
            <span class="icon">+</span>
          </div>
          <div class="card-content">
            <div class="card-content-inner">
              <p>
                Desarrollo de sitios web corporativos y tiendas en l&iacute;nea
                con cat&aacute;logos interactivos. Automatizamos el proceso
                inicial de venta: el cliente explora, filtra tallas, colores o
                disponibilidad y env&iacute;a el pedido listo para pagar
                directamente a tu WhatsApp, ahorr&aacute;ndote horas de
                respuesta manual.
              </p>
              <div class="solution-bullets">
                <div class="solution-bullet">
                  <span class="bullet-check">&#10003;</span>
                  <span
                    >Cat&aacute;logos con selecci&oacute;n y filtrado
                    autom&aacute;tico de producto</span
                  >
                </div>
                <div class="solution-bullet">
                  <span class="bullet-check">&#10003;</span>
                  <span
                    >Finalizaci&oacute;n de compra por WhatsApp sin costos de
                    comisi&oacute;n por pasarela</span
                  >
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- 4. Digitalización Integral para Negocios -->
        <div class="solution-card">
          <div class="card-header" onclick="toggleCard(this)">
            <div class="header-info">
              <span class="solution-chip"
                >Transformaci&oacute;n &amp; Automatizaci&oacute;n</span
              >
              <span class="solution-title"
                >Digitalizaci&oacute;n Integral para Negocios y Emprendedores</span
              >
            </div>
            <span class="icon">+</span>
          </div>
          <div class="card-content">
            <div class="card-content-inner">
              <p>
                Acompa&ntilde;amos a emprendedores y negocios en su
                transici&oacute;n al ecosistema digital. Orientamos la presencia
                de tu marca hacia modelos rentables de bajo costo por
                tr&aacute;fico controlado, automatizando procesos repetitivos y
                creando activos digitales escalables.
              </p>
              <div class="solution-bullets">
                <div class="solution-bullet">
                  <span class="bullet-check">&#10003;</span>
                  <span
                    >Estrategias adaptadas al presupuesto de emprendedores</span
                  >
                </div>
                <div class="solution-bullet">
                  <span class="bullet-check">&#10003;</span>
                  <span
                    >Integraci&oacute;n de herramientas digitales y soporte
                    estrat&eacute;gico continuo</span
                  >
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Modal Overlay -->
    <div id="contact-modal" class="modal-overlay">
      <div class="modal-content">
        <button class="modal-close" id="modal-close">X</button>

        <h2>Cont&aacute;ctanos</h2>

        <form id="modal-contact-form">
          <label>
            Nombre
            <input type="text" name="nombre" required />
          </label>

          <label>
            Correo electr&oacute;nico
            <input type="email" name="email" required />
          </label>

          <label>
            WhatsApp/Tel&eacute;fono
            <input type="tel" name="telefono" required />
          </label>

          <label>
            Medio de contacto de preferencia
            <select name="medio_contacto" required>
              <option value="" disabled selected
                >Seleccione una opci&oacute;n</option
              >
              <option value="WhatsApp">WhatsApp</option>
              <option value="Correo">Correo Electr&oacute;nico</option>
            </select>
          </label>

          <button type="submit" class="send-btn">Enviar</button>
        </form>
        <p id="modal-status" style="margin-top:10px; color:#f5c400;"></p>
      </div>
    </div>
  </body>
</html>

<style>
  @import url("https://fonts.googleapis.com/css2?family=Geist:wght@100&display=swap");

  html {
    scroll-behavior: smooth;
  }

  body {
    margin: 0;
    font-family: "Geist", sans-serif;
    background: #000;
    color: #fff;
  }

  /* HERO */
  .hero-section {
    position: relative;
    z-index: 10;
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 100vh;
    padding: 120px 2rem 2rem 2rem; /* Adds top space to prevent hiding under header */
    background-color: transparent;
    text-align: center;
    box-sizing: border-box;
  }

  #global-canvas {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 0;
    pointer-events: none;
    background: #000;
  }

  .hero-content {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-start;
    max-width: 800px;
    margin: 0 auto;
    color: rgb(255, 255, 255);
    gap: 1.5rem;
    text-align: left;
  }

  .hero-content h3 {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 900;
    line-height: 1.2;
    margin: 0;
  }

  .soluciones-section h3 {
    font-size: 3rem;
    font-weight: 300;
    margin-bottom: 2rem;
  }

  .cta-button {
    position: relative;
    background: rgba(0, 0, 0, 0.3);
    border: 2px solid #f1cd00;
    padding: 1.2rem 2.5rem;
    font-size: 1.4rem;
    font-weight: bold;
    color: #f1cd00;
    cursor: pointer;
    transition: all 0.4s ease;
    border-radius: 50px;
    text-decoration: none;
    text-transform: uppercase;
    letter-spacing: 2px;
    margin: 0;
    width: auto;
    overflow: hidden;
    backdrop-filter: blur(5px);
    box-shadow:
      0 0 10px rgba(241, 205, 0, 0.2),
      inset 0 0 10px rgba(241, 205, 0, 0.1);
  }

  .cta-button::after {
    content: "";
    position: absolute;
    top: 0;
    left: -100%;
    width: 50%;
    height: 100%;
    background: linear-gradient(
      to right,
      transparent,
      rgba(255, 255, 255, 0.3),
      transparent
    );
    transform: skewX(-20deg);
    transition: left 0.6s ease;
  }

  .cta-button:hover {
    background-color: #f1cd00;
    color: #000;
    box-shadow:
      0 0 20px rgba(241, 205, 0, 0.6),
      0 0 40px rgba(241, 205, 0, 0.4);
    transform: translateY(-3px);
  }

  .cta-button:hover::after {
    left: 150%;
  }

  /* SECCIÓN DE IMPACTO, ESTADÍSTICAS Y CASOS DE ÉXITO */
  .stats-section {
    position: relative;
    z-index: 10;
    overflow: visible;
    background: transparent;
    padding: 6rem 2rem;
    max-width: 1280px;
    margin: 0 auto;
    box-sizing: border-box;
  }

  .stats-header {
    text-align: center;
    max-width: 800px;
    margin: 0 auto 3.5rem auto;
  }

  .stats-badge {
    display: inline-block;
    background: rgba(241, 205, 0, 0.1);
    border: 1px solid rgba(241, 205, 0, 0.4);
    color: #f1cd00;
    font-size: 0.85rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 1.5px;
    padding: 0.4rem 1.2rem;
    border-radius: 50px;
    margin-bottom: 1.2rem;
    backdrop-filter: blur(8px);
  }

  .stats-header h2 {
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 900;
    color: #ffffff;
    margin: 0 0 1rem 0;
    line-height: 1.2;
  }

  .stats-subtitle {
    font-size: 1.15rem;
    color: #b0b0b0;
    line-height: 1.6;
    margin: 0;
  }

  .stats-grid {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 2rem;
    width: 100%;
    box-sizing: border-box;
  }

  .stat-card {
    background: rgba(10, 10, 10, 0.85);
    backdrop-filter: blur(14px);
    -webkit-backdrop-filter: blur(14px);
    border: 1px solid rgba(241, 205, 0, 0.25);
    border-radius: 14px;
    padding: 2.2rem 1.8rem;
    text-align: left;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    transition: all 0.35s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.6);
    position: relative;
    overflow: hidden;
  }

  .stat-card::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 3px;
    background: linear-gradient(90deg, transparent, #f1cd00, transparent);
    opacity: 0;
    transition: opacity 0.35s ease;
  }

  .stat-card:hover {
    transform: translateY(-7px);
    border-color: rgba(241, 205, 0, 0.7);
    box-shadow:
      0 18px 40px rgba(0, 0, 0, 0.8),
      0 0 25px rgba(241, 205, 0, 0.18);
  }

  .stat-card:hover::before {
    opacity: 1;
  }

  .stat-card-top {
    margin-bottom: 1.5rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    padding-bottom: 1.5rem;
  }

  .card-chip {
    display: inline-block;
    font-size: 0.75rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 1px;
    color: #f1cd00;
    background: rgba(241, 205, 0, 0.1);
    border: 1px solid rgba(241, 205, 0, 0.3);
    padding: 0.25rem 0.75rem;
    border-radius: 20px;
    margin-bottom: 1rem;
  }

  .stat-metric {
    font-size: 2.8rem;
    font-weight: 900;
    color: #f1cd00;
    line-height: 1;
    margin-bottom: 0.3rem;
    letter-spacing: -1px;
    text-shadow: 0 0 20px rgba(241, 205, 0, 0.35);
  }

  .stat-metric-label {
    font-size: 0.9rem;
    font-weight: 600;
    color: #e0e0e0;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .stat-card-body h4 {
    font-size: 1.35rem;
    font-weight: 800;
    color: #ffffff;
    margin: 0 0 0.8rem 0;
    line-height: 1.3;
  }

  .stat-card-body p {
    font-size: 0.98rem;
    color: #bbb;
    margin: 0 0 1.5rem 0;
    line-height: 1.6;
  }

  .stat-features {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
    background: rgba(255, 255, 255, 0.03);
    padding: 1rem;
    border-radius: 8px;
    border: 1px solid rgba(255, 255, 255, 0.05);
  }

  .feature-item {
    display: flex;
    align-items: flex-start;
    gap: 0.6rem;
    font-size: 0.88rem;
    color: #e5e5e5;
    line-height: 1.4;
  }

  .check-icon {
    color: #f1cd00;
    font-weight: bold;
    flex-shrink: 0;
  }

  /* SOLUCIONES RENOVADAS */
  .soluciones-section {
    position: relative;
    z-index: 10;
    background: transparent;
    color: white;
    text-align: center;
    padding: 6rem 2rem;
    max-width: 1100px;
    margin: 0 auto;
    box-sizing: border-box;
  }

  .soluciones-header {
    max-width: 800px;
    margin: 0 auto 3.5rem auto;
  }

  .soluciones-badge {
    display: inline-block;
    background: rgba(241, 205, 0, 0.1);
    border: 1px solid rgba(241, 205, 0, 0.4);
    color: #f1cd00;
    font-size: 0.85rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 1.5px;
    padding: 0.4rem 1.2rem;
    border-radius: 50px;
    margin-bottom: 1.2rem;
    backdrop-filter: blur(8px);
  }

  .soluciones-section h2 {
    font-size: clamp(2rem, 4vw, 3rem);
    font-weight: 900;
    margin: 0 0 1rem 0;
    color: #ffffff;
    line-height: 1.2;
  }

  .soluciones-subtitle {
    font-size: 1.15rem;
    color: #b0b0b0;
    line-height: 1.6;
    margin: 0;
  }

  .solution-cards {
    max-width: 900px;
    margin: 0 auto;
    text-align: left;
    display: flex;
    flex-direction: column;
    gap: 1.2rem;
  }

  .solution-card {
    background: rgba(10, 10, 10, 0.85);
    backdrop-filter: blur(14px);
    -webkit-backdrop-filter: blur(14px);
    border: 1px solid rgba(241, 205, 0, 0.25);
    border-radius: 12px;
    overflow: hidden;
    transition: all 0.35s cubic-bezier(0.4, 0, 0.2, 1);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.5);
  }

  .solution-card:hover {
    border-color: rgba(241, 205, 0, 0.6);
    box-shadow:
      0 12px 35px rgba(0, 0, 0, 0.7),
      0 0 20px rgba(241, 205, 0, 0.12);
  }

  .solution-card.open {
    border-color: #f1cd00;
    box-shadow:
      0 15px 40px rgba(0, 0, 0, 0.8),
      0 0 25px rgba(241, 205, 0, 0.2);
  }

  .card-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.3rem 1.8rem;
    cursor: pointer;
    background: rgba(15, 15, 15, 0.9);
    transition: background 0.3s ease;
    user-select: none;
  }

  .card-header:hover {
    background: rgba(30, 26, 12, 0.95);
  }

  .header-info {
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
  }

  .solution-chip {
    font-size: 0.75rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 1px;
    color: #f1cd00;
  }

  .solution-title {
    font-size: 1.25rem;
    font-weight: 800;
    color: #ffffff;
    letter-spacing: 0.3px;
  }

  .icon {
    font-size: 1.8rem;
    font-weight: 300;
    color: #f1cd00;
    transition: transform 0.35s cubic-bezier(0.4, 0, 0.2, 1);
    line-height: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 32px;
    height: 32px;
    flex-shrink: 0;
  }

  .solution-card.open .icon {
    transform: rotate(45deg);
  }

  .card-content {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.4s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .solution-card.open .card-content {
    max-height: 500px;
  }

  .card-content-inner {
    padding: 1.5rem 1.8rem 1.8rem 1.8rem;
    border-top: 1px solid rgba(241, 205, 0, 0.15);
    background: rgba(5, 5, 5, 0.6);
  }

  .card-content-inner p {
    font-size: 1rem;
    color: #cccccc;
    margin: 0 0 1.2rem 0;
    line-height: 1.6;
  }

  .solution-bullets {
    display: flex;
    flex-direction: column;
    gap: 0.7rem;
    background: rgba(255, 255, 255, 0.03);
    padding: 1rem 1.2rem;
    border-radius: 8px;
    border: 1px solid rgba(255, 255, 255, 0.06);
  }

  .solution-bullet {
    display: flex;
    align-items: flex-start;
    gap: 0.6rem;
    font-size: 0.92rem;
    color: #e0e0e0;
    line-height: 1.5;
  }

  .solution-bullet strong {
    color: #f1cd00;
  }

  .bullet-check {
    color: #f1cd00;
    font-weight: bold;
    flex-shrink: 0;
  }

  /* Overlay oscuro */
  .modal-overlay {
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.4);
    backdrop-filter: blur(4px);
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 9999;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.3s ease;
  }

  .modal-overlay.active {
    opacity: 1;
    pointer-events: auto;
  }

  /* Caja del modal */
  .modal-content {
    box-sizing: border-box;
    background: rgba(10, 10, 10, 0.5);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    border: 1px solid rgba(241, 205, 0, 0.2);
    color: #fff;
    padding: 2rem 1.5rem;
    border-radius: 12px;
    width: 90%;
    max-width: 400px;
    max-height: 85vh;
    overflow-y: auto;
    position: relative;
    box-shadow:
      0 0 40px rgba(0, 0, 0, 0.8),
      inset 0 0 20px rgba(255, 255, 255, 0.05);
    animation: modalPop 0.35s ease;
  }

  @keyframes modalPop {
    from {
      transform: scale(0.8);
      opacity: 0;
    }
    to {
      transform: scale(1);
      opacity: 1;
    }
  }

  /* Botón cerrar */
  .modal-close {
    position: absolute;
    top: 12px;
    right: 12px;
    background: transparent;
    border: none;
    color: #fff;
    font-size: 1.5rem;
    cursor: pointer;
  }

  /* Inputs */
  .modal-content input,
  .modal-content select,
  .modal-content textarea {
    box-sizing: border-box;
    width: 100%;
    padding: 0.7rem;
    margin-top: 0.3rem;
    margin-bottom: 1rem;
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 6px;
    background: rgba(255, 255, 255, 0.05);
    color: #fff;
    transition:
      border-color 0.3s ease,
      background 0.3s ease;
  }

  .modal-content select {
    appearance: none;
    -webkit-appearance: none;
    cursor: pointer;
    background-image: url("data:image/svg+xml;charset=UTF-8,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none' stroke='white' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'%3e%3cpolyline points='6 9 12 15 18 9'%3e%3c/polyline%3e%3c/svg%3e");
    background-repeat: no-repeat;
    background-position: right 1rem center;
    background-size: 1em;
  }

  .modal-content select option {
    background: #111;
    color: #fff;
  }

  .modal-content input:focus,
  .modal-content select:focus,
  .modal-content textarea:focus {
    outline: none;
    border-color: #f1cd00;
    background: rgba(255, 255, 255, 0.1);
  }

  /* Botón enviar */
  .send-btn {
    background: #f5c400;
    color: #000;
    padding: 0.8rem 1.6rem;
    border: none;
    border-radius: 6px;
    cursor: pointer;
    font-weight: bold;
    width: 100%;
    transition: opacity 0.2s ease;
  }

  .send-btn:hover {
    opacity: 0.8;
    background-color: #fdf6f6;
  }

  /* Oculto */
  .hidden {
    display: none;
  }

  @media (max-width: 768px) {
    .hero-section {
      padding: 6rem 1rem 3rem 1rem;
    }

    .hero-content h3 {
      font-size: 2.5rem;
    }

    .cta-button {
      font-size: 1.1rem;
      padding: 1rem 2rem;
    }

    .stats-section {
      padding: 4rem 1.2rem;
    }

    .stats-header {
      margin-bottom: 2.5rem;
    }

    .stats-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .stat-card {
      padding: 1.8rem 1.4rem;
    }

    .stat-metric {
      font-size: 2.4rem;
    }

    .soluciones-section {
      padding: 4rem 1.2rem;
    }

    .soluciones-header {
      margin-bottom: 2.5rem;
    }

    .card-header {
      padding: 1.1rem 1.2rem;
    }

    .solution-title {
      font-size: 1.05rem;
    }

    .card-content-inner {
      padding: 1.2rem;
    }

    .solution-card {
      border: 1px solid rgba(241, 205, 0, 0.2); /* Much softer on mobile as requested */
    }
  }
</style>

<script type="module">
  // ---------------------------
  // Canvas Tech Animation
  // ---------------------------
  const canvas = document.getElementById("global-canvas");
  if (canvas) {
    const ctx = canvas.getContext("2d");
    let width, height;
    let particles = [];

    function initCanvas() {
      width = canvas.width = window.innerWidth;
      height = canvas.height = window.innerHeight;
      particles = [];
      const numParticles = Math.floor((width * height) / 8000);
      for (let i = 0; i < numParticles; i++) {
        particles.push({
          x: Math.random() * width,
          y: Math.random() * height,
          vx: (Math.random() - 0.5) * 1.2,
          vy: (Math.random() - 0.5) * 1.2,
          radius: Math.random() * 2 + 1,
        });
      }
    }

    function drawParticles() {
      ctx.clearRect(0, 0, width, height);
      ctx.fillStyle = "rgba(241, 205, 0, 0.8)";
      ctx.lineWidth = 0.5;

      for (let i = 0; i < particles.length; i++) {
        const p = particles[i];
        p.x += p.vx;
        p.y += p.vy;

        if (p.x < 0 || p.x > width) p.vx *= -1;
        if (p.y < 0 || p.y > height) p.vy *= -1;

        ctx.beginPath();
        ctx.arc(p.x, p.y, p.radius, 0, Math.PI * 2);
        ctx.fill();

        for (let j = i + 1; j < particles.length; j++) {
          const p2 = particles[j];
          const dist = Math.sqrt((p.x - p2.x) ** 2 + (p.y - p2.y) ** 2);
          if (dist < 120) {
            ctx.strokeStyle = `rgba(241, 205, 0, ${1 - dist / 120})`;
            ctx.beginPath();
            ctx.moveTo(p.x, p.y);
            ctx.lineTo(p2.x, p2.y);
            ctx.stroke();
          }
        }
      }
      requestAnimationFrame(drawParticles);
    }

    initCanvas();
    drawParticles();
    window.addEventListener("resize", initCanvas);
  }

  // ---------------------------
  // Toggle de tarjetas
  // ---------------------------
  function toggleCard(header) {
    const card = header.closest(".solution-card");
    if (card) {
      card.classList.toggle("open");
    }
  }
  window.toggleCard = toggleCard;

  // ---------------------------
  // Modal
  // ---------------------------
  const openBtn = document.querySelector("#open-modal-btn");
  const modal = document.querySelector("#contact-modal");
  const closeBtn = document.querySelector("#modal-close");
  const form = document.querySelector("#modal-contact-form");
  const statusEl = document.querySelector("#modal-status");

  openBtn.addEventListener("click", (e) => {
    e.preventDefault();
    modal.classList.add("active");
  });

  closeBtn.addEventListener("click", () => {
    modal.classList.remove("active");
  });

  modal.addEventListener("click", (e) => {
    if (e.target === modal) modal.classList.remove("active");
  });

  // ---------------------------
  // Google Sheets
  // ---------------------------
  const SCRIPT_URL =
    "https://script.google.com/macros/s/AKfycbz8YJmjhHiEXD-o3iYDErabiZ-vZITA7lXqTtAIUVHJL9hyDw4ATbqY7YX8FYEInsWW/exec";

  form.addEventListener("submit", async (event) => {
    event.preventDefault();

    statusEl.textContent = "Enviando...";

    const formData = new FormData(form);
    const data = Object.fromEntries(formData.entries());

    try {
      const res = await fetch(SCRIPT_URL, {
        method: "POST",
        headers: {
          "Content-Type": "text/plain;charset=utf-8",
        },
        body: JSON.stringify(data),
      });

      if (!res.ok) throw new Error("Error en servidor AS");

      statusEl.textContent = "Mensaje enviado! Pronto te contactaremos";

      form.reset();

      setTimeout(() => {
        modal.classList.remove("active");
        statusEl.textContent = "";
      }, 1500);
    } catch (err) {
      console.error(err);
      statusEl.textContent = "Error al enviar. Intenta de nuevo.";
    }
  });
</script>
