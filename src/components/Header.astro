<header class="header">
  <div class="header-left">
    <a href="/Tech-Crew/" class="logo-link" aria-label="Tech Crew Inicio">
      <img src="/Tech-Crew/tc.png" alt="Logo de Tech Crew" class="logo" />
    </a>
  </div>

  <div class="header-right">
    <!-- Enlace directo a WhatsApp: Puedes configurar tu número (ej: https://wa.me/506XXXXXXXX?text=...) -->
    <a
      id="header-whatsapp-btn"
      href="https://wa.me/?text=Hola%20Tech%20Crew,%20quiero%20m%C3%A1s%20informaci%C3%B3n%20sobre%20sus%20soluciones%20digitales"
      target="_blank"
      rel="noopener noreferrer"
      class="whatsapp-btn"
      aria-label="Iniciar chat por WhatsApp"
    >
      <svg class="whatsapp-icon" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
        <path d="M12.031 6.172c-3.181 0-5.767 2.586-5.768 5.766-.001 1.298.38 2.27 1.019 3.287l-.711 2.598 2.664-.699c.97.527 1.834.78 2.796.78 3.184 0 5.77-2.587 5.77-5.766.001-3.187-2.575-5.77-5.77-5.77zm0 10.457c-.854 0-1.636-.237-2.316-.649l-.166-.1-1.581.415.422-1.541-.109-.174c-.452-.721-.69-1.564-.69-2.428 0-2.493 2.029-4.521 4.523-4.521 2.494 0 4.524 2.028 4.525 4.521 0 2.495-2.03 4.524-4.524 4.524zM12 2C6.477 2 2 6.477 2 12c0 1.891.524 3.662 1.438 5.18L2 22l4.975-1.305A9.957 9.957 0 0 0 12 22c5.523 0 10-4.477 10-10S17.523 2 12 2z"/>
      </svg>
      <span class="whatsapp-text">Chat por WhatsApp</span>
    </a>
  </div>
</header>

<style>
  /* ---------- ESTILOS GENERALES DEL HEADER ---------- */
  .header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 2rem;
    background: rgba(0, 0, 0, 0.85);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 75px; /* Altura estandarizada del componente menú */
    z-index: 1000;
    box-sizing: border-box;
    border-bottom: 1px solid rgba(241, 205, 0, 0.15);
    overflow: visible; /* Permite que el logo sobresalga y flote libremente */
  }

  /* ---------- ZONA IZQUIERDA: LOGO SUPERPUESTO (140px, top 15px) ---------- */
  .header-left {
    position: absolute;
    left: 2rem;
    top: 15px; /* Separación de 15px del borde superior */
    display: flex;
    align-items: center;
    z-index: 1002;
    pointer-events: auto;
  }

  .logo-link {
    display: flex;
    align-items: center;
    text-decoration: none;
  }

  /* Logo agrandado y superpuesto a la izquierda */
  .logo {
    height: 140px; /* Medida original de 140px */
    width: auto;
    display: block;
    object-fit: contain;
    filter: drop-shadow(0px 6px 16px rgba(0, 0, 0, 0.9));
    transition:
      transform 0.3s cubic-bezier(0.4, 0, 0.2, 1),
      filter 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .logo:hover {
    transform: scale(1.05);
    filter: drop-shadow(0px 0px 25px rgba(241, 205, 0, 0.8)) drop-shadow(0px 6px 16px rgba(0, 0, 0, 0.9));
  }

  /* ---------- ZONA DERECHA: BOTÓN CHAT POR WHATSAPP ---------- */
  .header-right {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    margin-left: auto;
    position: relative;
    z-index: 1005;
  }

  .whatsapp-btn {
    position: relative;
    display: inline-flex;
    align-items: center;
    gap: 0.65rem;
    background: rgba(0, 0, 0, 0.4);
    border: 1.5px solid #f1cd00;
    padding: 0.65rem 1.6rem;
    font-size: 1rem;
    border-radius: 50px;
    color: #f1cd00;
    cursor: pointer;
    transition: all 0.4s ease;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 1px;
    overflow: hidden;
    backdrop-filter: blur(6px);
    box-shadow:
      0 0 10px rgba(241, 205, 0, 0.2),
      inset 0 0 10px rgba(241, 205, 0, 0.08);
    text-decoration: none;
  }

  .whatsapp-icon {
    width: 20px;
    height: 20px;
    fill: currentColor;
    flex-shrink: 0;
    transition: transform 0.3s ease;
  }

  .whatsapp-btn::after {
    content: "";
    position: absolute;
    top: 0;
    left: -100%;
    width: 50%;
    height: 100%;
    background: linear-gradient(
      to right,
      transparent,
      rgba(255, 255, 255, 0.3),
      transparent
    );
    transform: skewX(-20deg);
    transition: left 0.6s ease;
  }

  .whatsapp-btn:hover {
    background-color: #f1cd00;
    color: #000;
    box-shadow:
      0 0 18px rgba(241, 205, 0, 0.6),
      0 0 35px rgba(241, 205, 0, 0.4);
    transform: translateY(-2px);
  }

  .whatsapp-btn:hover .whatsapp-icon {
    transform: scale(1.15);
  }

  .whatsapp-btn:hover::after {
    left: 150%;
  }

  /* ---------- REGLAS RESPONSIVAS ---------- */
  @media (max-width: 768px) {
    .header {
      height: 65px;
      padding: 0 1.2rem;
    }

    .header-left {
      left: 1.2rem;
      top: 10px;
    }

    .logo {
      height: 105px; /* Medida superpuesta en tablet */
    }

    .whatsapp-btn {
      padding: 0.5rem 1.1rem;
      font-size: 0.85rem;
      gap: 0.45rem;
    }

    .whatsapp-icon {
      width: 17px;
      height: 17px;
    }
  }

  @media (max-width: 480px) {
    .header {
      height: 60px;
      padding: 0 0.8rem;
    }

    .header-left {
      left: 0.8rem;
      top: 8px;
    }

    .logo {
      height: 88px; /* Medida superpuesta en móvil */
      max-width: 160px;
    }

    .whatsapp-btn {
      padding: 0.45rem 0.85rem;
      font-size: 0.78rem;
      letter-spacing: 0.5px;
    }

    .whatsapp-icon {
      width: 15px;
      height: 15px;
    }
  }
</style>
