<header class="header">
   <div class="logo">
      <img src="/tc.png" alt="Logo de Tech Crew" />
   </div>
   <nav class="nav-container">
      <ul class="nav-links">
         <li><a href="/">Tech Crew!</a></li>
         <li><a href="/TechCrew">Podcast</a></li>
         <li><a href="/Contacto">Contacto</a></li>
      </ul>
   </nav>
</header>

<style>
   @import url('https://fonts.googleapis.com/css2?family=Geist:wght@100&display=swap');

   /* Estilos generales para el header */
   .header {
      display: flex;
      align-items: center;
      padding: 1rem 2rem;
      background-color: black;
      border-bottom: 1px solid #000000;
   }

   .logo {
      display: flex;
      align-items: center;
      margin-right: auto;
   }

   .logo img {
      height: 150px;
      margin-right: 10px;
   }

   .nav-container {
       flex: 1; 
       display: flex;
       justify-content: left;
   }

   .nav-links {
      list-style: none;
      display: flex;
      gap: 250px;
      margin-right: 0;
   }

   .nav-links a {
      text-decoration: none;
      color: #ffffff;
      font-family: 'Geist', sans-serif;
      font-size: 25px;
      padding-bottom: 10px; 
      border-bottom: 1px solid transparent;
      transition: border-bottom-color 0.3s ease;
   }
   
   .nav-links a:hover {
      color: #366499;
      border-bottom-color: #ffffff;
   }

   /* Responsividad para pantallas medianas (tabletas) */
   @media (max-width: 768px) {
      .header {
         flex-direction: column; /* Cambia la disposición a columna en tabletas */
         padding: 1rem;
      }

      .logo img {
         height: 120px; /* Reduce el tamaño del logo */
         margin-bottom: 1rem;
      }

      .nav-links {
         gap: 50px; /* Reduce el espacio entre enlaces */
      }

      .nav-links a {
         font-size: 20px; /* Reduce el tamaño de la fuente */
      }
   }

   /* Responsividad para pantallas pequeñas (móviles) */
   @media (max-width: 480px) {
      .header {
         padding: 1rem;
      }

      .logo img {
         height: 100px;
      }

      .nav-links {
         flex-direction: column; /* Coloca los enlaces en columna en móviles */
         gap: 20px;
         margin: 0;
      }

      .nav-links a {
         font-size: 18px; /* Reduce aún más el tamaño de la fuente */
         text-align: center;
      }
   }
</style>
